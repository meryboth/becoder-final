<!-- profile.handlebars -->
<section class="flex justify-center items-center h-screen bg-gray-100">
  <div class="bg-white p-8 rounded shadow-md w-full sm:w-96">
    <h1 class="text-3xl font-semibold mb-4">Mi Perfil</h1>
    <p class="mb-4">Bienvenido!, {{user.first_name}} {{user.last_name}}!</p>
    <p class="mb-4">Email: {{user.email}}</p>
    <p class="mb-4">Edad: {{user.age}}</p>
    <a href="/api/sessions/logout" class="text-indigo-500 hover:text-indigo-600">Logout</a>
  </div>
</section>
